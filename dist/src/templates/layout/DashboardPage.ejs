import { resourceRegistry } from '../panel-resources/_registry';

export default function AdminDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">Welcome to your Dashboard</h1>
      
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        {/* Welcome Widget */}
        <div className="p-6 border rounded-xl bg-gradient-to-br from-blue-500 to-purple-600 text-white">
          <h2 className="text-lg font-semibold">Hello, Admin!</h2>
          <p className="opacity-80 text-sm">System is running smoothly.</p>
        </div>

        {/* Resource Stats Widgets */}
        {resourceRegistry.map(resource => (
          <div key={resource.slug} className="p-6 border rounded-xl hover:shadow-md transition">
            <h3 className="font-medium text-gray-500">{resource.name}</h3>
            <p className="text-2xl font-bold mt-2">Manage</p>
            <a href={`/admin/${resource.slug}`} className="text-blue-600 text-sm mt-4 inline-block">
              View All →
            </a>
          </div>
        ))}
      </div>
    </div>
  );
}