import { resourceRegistry } from '<%- resolveImport("panel-resources") %>';

export default function AdminDashboard() {
  return (
    <div>
      <h1 className="<%= hasTailwind ? 'text-2xl font-bold mb-6 text-gray-800' : '' %>">
        Welcome to your Dashboard
      </h1>
      
      <div className="<%= hasTailwind ? 'grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6' : 'admin-grid' %>">
        {/* Welcome Widget */}
        <div className="<%= hasTailwind ? 'p-6 border rounded-xl bg-gradient-to-br from-indigo-600 to-blue-500 text-white shadow-sm' : '' %>">
          <h2 className="text-lg font-semibold">System Status</h2>
          <p className="opacity-90 text-sm mt-1">NextJS Panel v2.0 is active and connected to Prisma.</p>
        </div>

        {/* Dynamic Resource Cards */}
        {resourceRegistry.map((resource<% if (isTs) { %>: { name: string; slug: string }<% } %>) => (
          <div 
            key={resource.slug} 
            className="<%= hasTailwind ? 'p-6 border rounded-xl bg-white hover:shadow-md transition-shadow border-gray-200' : '' %>"
          >
            <h3 className="font-medium text-gray-500 uppercase text-xs tracking-wider">
              Resource
            </h3>
            <p className="text-2xl font-bold mt-1 capitalize text-gray-900">
              {resource.name}
            </p>
            <a 
              href={`/admin/${resource.slug}`} 
              className="text-indigo-600 text-sm font-medium mt-4 inline-block hover:underline"
            >
              Manage Data →
            </a>
          </div>
        ))}
      </div>
    </div>
  );
}